<% render "shared/html" %>

  <title>All Users</title>
</head>
<body>

<%= render "shared/toolbar" %>

<!-- this will just be my blog after GA; otherwise this would be a  -->
<div class="user-list">
<p>Welcome to <span id="inline-span">Void.shout()</span>! This is a blog that I created near the end of the <a href="https://generalassemb.ly/">General Assembly's</a> <a href="https://generalassemb.ly/education/web-development-immersive">Web Development Immersive</a> as my mini-Rails project. Over time the personality of this blog is gonna evolve. Maybe there'll just me me thinking, maybe a vlog of what I'm working on; doesn't matter where my writing ends up as long as it's out there!
<br><br>
My <a href="github.com/SiouxsieAsylum">GitHub!</a>
<br><br>
</p>

<h3>Users</h3>

<ul>
  <% @users.each do |user| %>
    <li><%= link_to user.username, user_path(user), class: "link"  %></li>
  <% end %>
</ul>
</div>

<div id="feed">
<% if @posts != [] %>

<ul>
  <a href="<%= new_post_path %>">&#9998; New Post</a>
  <% @posts.reverse_each do |post| %>

    <li>
      <div class="index-post">
        <div class="index-post-time">
          Posted on <%= post.created_at.strftime("%m-%d-%y") %>
        </div>
        <div class="content">
          <%= post.body %>
        </div>
      </div>
    </li>
    <% end %>
</ul>
<% else %>

  <p>Welcome! <%= link_to "Click here to post!", new_post_path, class: "link"  %></p>

<%end%>
</div>


<%= render "shared/footer" %>


